<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rcpl.ocr">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addFromScan"></activity>
        <activity android:name=".viewblacklist" />
        <activity
            android:name=".viewwhitelist"
            android:label="@string/title_activity_whitelist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".blacklist" />
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreeen" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>